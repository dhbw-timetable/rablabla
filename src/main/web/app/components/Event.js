import React from 'react';

export default function Event() {
  const data = {};

  return (
    <li>
        <p>{data}</p>
        <h3>{data}</h3>
        <p>{data}</p>
        <p>{data}</p>
    </li>
  );
}
